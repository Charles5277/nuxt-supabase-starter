# \do-all（relay）— \nx 三項

開工：2026-08-19 04:20
前一段：`tasks/2026-08-19-0303-td007-td006.md`（TD-006 / TD-007 已結案並 push 到 `383e240`）

## 三項

- T1 `starter-public-hygiene-commands` spectra change（0/28，跨 root meta 與 `template/` 兩層）→ 需乾淨 session
- T2 TD-005 pre-push 7 道 check 在 meta-monorepo 全部靜默 no-op → 落點在 clade `vendor/scripts/pre-push/`，需 clade session
- T3 `add-evlog-baseline-and-scaffolder-preset-flag` tasks.md 的 2.7 打勾（e2e 測試檔已存在）→ 單檔，主線當下做

## Edge 判定

真 edge：**零**。三項兩兩之間都不需要讀對方產出——
T1 動 `template/openspec/changes/starter-public-hygiene-commands/` 與投影面存量、
T2 動 clade repo、T3 動另一條 change 的 tasks.md。

一條**排序約束（非 edge）**：T3 要在 relay 之前 commit 完。理由是工作區競用，不是資料依賴——
successor 在本 repo 開工時若還有未 commit 改動，會重演 2026-08-19 那兩次資料遺失
（clade `docs/pitfalls/2026-08-19-working-tree-contention-escalated-without-session-layer-probe.md`）。

## 派工判定

- T3：主線自己做。單檔勾選，沒命中任何外派條件。
- T1 + T2：命中「需要獨立 session」→ 走 Herdr relay transport，一份 brief 交一個 successor，
  由它以 fresh context 判要不要把 T2 再細分派去 clade。不是 subagent fan-out。

## 進度

- [x] T3 打勾 2.7 + commit（`27fc919`，已 push）
- [x] 寫 relay brief：`/home/charles/.claude-work/briefs/2026-08-19-starter-do-all-relay.md`
- [ ] `/handoff relay` 交出位置

## 訂正一條

`\nx` 時把 T1 的 scope 講成含「投影面存量去識別化」，讀 proposal 後確認不是：
T1 是 **L3 commands 層**（9 個 starter-owned command 的 keep / sanitize / remove ＋
`validate-starter` 移回 root ＋ 新 audit script）。投影面散文的真實 consumer 名去識別化
屬 clade 的 `clade-starter-sanitization`（sister change，brief 在該 change 目錄內）。
