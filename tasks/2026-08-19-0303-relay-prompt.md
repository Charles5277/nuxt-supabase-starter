你接手 `nuxt-supabase-starter` 的 TD-007 + TD-006。前一個 session（cc 帳號）額度用盡，把整個位置交給你，它已收工。

cwd = `/home/charles/offline/nuxt-supabase-starter`（starter seed 在 `template/`）。

## 先讀這三份（依序，不要重新調查已寫好的東西）

1. `tasks/2026-08-19-0303-td007-td006.md` — **中斷點**：已完成並實測的項目、blocker、0-A gate 狀態、兩個推翻原計畫的實測發現。這份是你的主要狀態載體
2. `/home/charles/.claude-work/briefs/2026-08-19-starter-td006-td007.md` — 原始 brief（為什麼要獨佔窗口、已確認的事實、驗收條件）
3. `docs/tech-debt.md` 的 TD-007 與 TD-006 兩則 entry — Problem / Fix approach / Acceptance 已寫完

計畫在 `/home/charles/.claude/plans/declarative-cooking-quill.md`，但其中兩個前提已被實測推翻，以 `tasks/` 那份的「實測發現」段為準。

## 現況

工作區有 **4 個 modified、未 commit**：

- `template/package.json`（TD-006 postinstall self-detection）
- `scripts/audit-template-hygiene.sh`（TD-006 `check_clade_projection_consumer_names`）
- `scripts/audit-template-hygiene.test.sh`（對應 fixture test）
- `.claude/rules/starter-hygiene.md`（check name 表 + 覆蓋邊界段）

commit lock 已釋放、無 worktree、無 stash、無 background job。（訂正：`tasks/` **未**被 gitignore，其中的檔案是 tracked，要照常納入 commit。）

## 依序做

1. **修 blocker**：`bash scripts/audit-template-hygiene.test.sh` 印出全部 `ok` 與 `All ... passed`，但 **exit code 是 127**。複驗：`bash scripts/audit-template-hygiene.test.sh >/dev/null 2>&1; echo $?`。懷疑是新增的 `assert_clade_projection_consumer_names` 內 `source "${AUDIT_SCRIPT}"` 之後的 subshell 環境（該 script 自帶 `set -euo pipefail` 與 EXIT trap）。**NEVER** 在修好之前 commit——印 pass 卻 exit 非 0 的測試比沒有測試更糟
2. **跑 `/commit`** 把 TD-006 這批進版。注意 0-A.1 的 codex review 在前一輪回 `quota blocked`（cursor 備援池也回 `No models match pattern`），你這輪要重跑一次確認；仍 blocked 就依 fallback chain 主線自審並在收尾如實回報「codex review 沒有跑到」
3. **TD-007**：postinstall guard 已就位，現在跑 `npx vitest run`（在 `template/packages/create-nuxt-starter/`）才安全。用它重現並行失敗——`pnpm vp test run` 不加 `--no-file-parallelism` **本來就全綠**（92 passed / 1 skipped），因為本 workspace 的 `vitest` 是 `@voidzero-dev/vite-plus-test` 別名，跟上游 vitest 是不同 runner。重現後再修 `src/cli.ts` 的 `detectMonorepoRoot()`（第 42–61 行，`process.env.PWD` → `process.cwd()` 為主）與 `getInvocationCwd()`（第 63–80 行同型），以及各測試檔的 `TEST_DIR`（`test/.tmp-*` → `mkdtempSync(tmpdir())`）
4. **收尾**：兩則 TD entry 改 Status + 留驗收輸出摘要。TD-006 額外補記這條邊界——full-tree audit 的 `find -prune` 本來就排除 `template/.claude` / `.agents` / `.codex`，所以新檢查的載體是 **pre-commit ratchet**，full audit 維持綠；既有污染仍歸 `starter-public-hygiene-commands` / `clade-starter-sanitization`（2026-08-19 重測基線：`template/.claude` 13 / 895、`template/.agents` 17 / 821、`template/.codex` 0 / 9；`12 / 853` 是舊邊界下的過期數字）

## 紀律

- 每完成一段就 commit，不累積未 commit 改動——TD-007 的驗收本身就會觸發清空工作區的行為
- 逐條實跑驗收並貼輸出，**NEVER** 為了趕進度跳過驗收
- 開工前先跑一次 session 層探測確認沒有別的 agent 在這個 repo 工作（`ListAgents` + `pgrep -af "work-loop|runner.sh|--unattended"` + `git status` / `git worktree list` / `git stash list`）
