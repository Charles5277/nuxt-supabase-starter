# TD-007 + TD-006

開工：2026-08-19 03:03
Brief：`/home/charles/.claude-work/briefs/2026-08-19-starter-td006-td007.md`
Plan：`/home/charles/.claude/plans/declarative-cooking-quill.md`

## 前置（已完成）

- session 層探測：`git status` 乾淨、無 worktree / stash、work-loop 全在 perno；wM:p2 兩度回覆已無未 commit 改動 → 獨佔窗口乾淨
- 存量處置已拍板：full-tree audit 允許轉紅，不加 baseline allowlist

## 進度

## 實測發現（改變了排序）

- `pnpm vp test run`（不加 `--no-file-parallelism`）**本來就全綠**：92 passed / 1 skipped
  （`nuxthub-d1-migration-smoke` 條件跳過）。TD-007 記的失敗是 `npx vitest run`——本 workspace
  的 `vitest` 是 `@voidzero-dev/vite-plus-test` 別名，`npx vitest` 會另外抓上游 vitest，是不同 runner。
  而 `npx vitest` 的安裝步驟正是 TD-006 的污染入口 → **先做 TD-006，再用 `npx vitest run` 重現 TD-007**。
- full-tree audit 的 `find -prune` 本來就排除 `template/.claude` / `.agents` / `.codex`，所以
  TD-006 的新檢查載體是 pre-commit hook（ratchet），full audit 維持綠。既有污染 12 / 853 檔。

- [x] TD-007 step 1：重現並行失敗（`pnpm vp test run` 不加 flag）→ 不重現，見上
- [x] TD-007 step 2：`cli.ts` `detectMonorepoRoot()` / `getInvocationCwd()` 改以 `process.cwd()` 為主
- [x] TD-007 step 3：test `TEST_DIR` 移到 `mkdtempSync(tmpdir())`
- [x] TD-007 step 4：驗收 + commit
- [x] TD-006 step 1：`template/package.json` postinstall self-detection（install 兩處實跑，`template/.claude/` 零變更）
- [x] TD-006 step 2：`check_tenant_identifiers()` 涵蓋 clade 投影面（probe 檔實測擋下 + 負向控制放行）
- [x] TD-006 step 3：rule check-name 表 + 覆蓋邊界段 + fixture test（含 mutation 證明）
- [x] TD-006 step 4：驗收 + commit
- [x] 收尾：兩則 TD entry Status + 驗收摘要

## 額度中斷點（2026-08-19 03:1x）

工作區狀態：4 個 modified、**未 commit**（`.claude/rules/starter-hygiene.md`、
`scripts/audit-template-hygiene.sh`、`scripts/audit-template-hygiene.test.sh`、
`template/package.json`）。commit lock 已釋放。（訂正：`tasks/` **未**被 gitignore——本檔是 tracked、relay-prompt 是 untracked 且非 ignored，照 commit skill 0-Scope 兩個都納入。）

### 必須先修的一條（blocker，接手第一件事）

`bash scripts/audit-template-hygiene.test.sh` **印出全部 `ok` 與 "All ... passed"，
但 exit code 是 127**。先前兩次量測用 `... | tail -3; echo "${PIPESTATUS[0]}"` 讀不到真值
（PIPESTATUS 已被覆寫），所以沒發現。127 = command not found，來源尚未定位；懷疑是新增的
`assert_clade_projection_consumer_names` 內 `source "${AUDIT_SCRIPT}"` 之後的 subshell 環境
（audit script 自帶 `set -euo pipefail` 與 trap）。**NEVER** 在修好之前 commit——一個
印 pass 卻 exit 非 0 的測試比沒有測試更糟。

複驗方式：`bash scripts/audit-template-hygiene.test.sh >/dev/null 2>&1; echo $?`

### 已完成且已實測（TD-006）

- postinstall self-detection：`template/` 與 `template/packages/create-nuxt-starter/` 各跑一次
  `pnpm install`，兩次都印 skip 訊息，`git status --porcelain -- template/.claude/` 零輸出
- `check_clade_projection_consumer_names`：probe 檔正向 exit 1（印四段 `real-tenant-identifier`
  報告）、負向（`<consumer-a>` + `_notion-tdms-board` + starter 自己的 GitHub URL）exit 0
- mutation 證明：把投影面 path case 改壞 → 新測試轉紅
- full-tree audit 維持綠（該 script 的 `find -prune` 本來就排除 `template/.claude`）
- rule check-name 表已更新 + 新增「clade 投影面的覆蓋邊界」段

### 0-A gate 狀態

- 0-A.0 simplify：四個角度由主線自跑（本 session 的操作約束禁止在使用者未要求時開 subagent）。
  修了兩處：`sed` subprocess → bash parameter expansion；抽 `CONSUMER_NAMES` 常數。
  抽常數時一度把 `yudefine` 併進 `check_dogfood_business_code`，造成 `template/app/pages/(home).vue`
  誤判（那裡的 YuDefine 是發佈本 starter 的 org），已還原並在兩處各留註解說明為何清單刻意不同
- 0-A.1 codex xhigh：**quota-blocked，review 沒有跑到**（`pi-codex-review: quota blocked`，
  cursor 備援池也回 `No models match pattern`）。依 fallback chain 落回 Opus 主線自審
- 0-C `pnpm run check`：oxlint 0 errors / 5 warnings（全是既有 `no-underscore-dangle`），
  typecheck 跑完無錯

## 備註

- 帳號額度：此 pane 已用 93%，3:20 重置。撞上限就停在當下、進度寫回 TD entry
- 每完成一段就 commit，不累積未 commit 改動

## 結案（2026-08-19，接手 session）

blocker（exit 127）在接手後複驗 4 次都是 exit 0，兩個假設（audit script 缺 source guard、
test 檔中間狀態）實測都不重現，判定為前一 session 編輯中途的暫態。改為從結構上消除該類失敗：
負向斷言改 `if` 形式、cleanup 失敗在 trap 內出聲、結尾核對實際 pass 數等於 assert 數。

commit：`b3dd3b0`（TD-006 fix）、`409091e`（task 檔）、`c7a0c5b`（TD-007 fix）、`a9006af`（TD entry）。
codex review 走 cursor 池共 6 輪，最後兩輪 No findings（預設池整段期間 quota blocked）。
未 push——root repo 沒有 package.json，`/commit` Step 6 的版本號升級不適用，push 留給使用者決定。
